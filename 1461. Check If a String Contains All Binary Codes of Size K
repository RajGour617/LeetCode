class Solution {
    public boolean hasAllCodes(String s, int k) {
        HashSet<String> binCode = new HashSet<>();

        for (int i = 0; i + k <= s.length(); i++) {
            binCode.add(s.substring(i, i + k));
        }

        return binCode.size() == Math.pow(2, k);
    }
}
